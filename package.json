{
  "name": "ai_skin",
  "version": "1.0.0",
  "private": true,
  "description": "AI skincare full-stack application with Express backend and Vue frontend.",
  "scripts": {
    "install:all": "npm ci --prefix backend && npm ci --prefix frontend",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "VUE_APP_API_URL=http://localhost:5000/api npm --prefix frontend run serve -- --host 0.0.0.0 --port 8080",
    "start:backend": "npm --prefix backend start",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix backend test",
    "check": "npm run test && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}

